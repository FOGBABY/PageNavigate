<Window x:Class="PageNavigate.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PageNavigate"
        xmlns:vm="clr-namespace:PageNavigate.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--Window.DataContext>
        <vm:NavigateViewModel></vm:NavigateViewModel>
    </Window.DataContext-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
        </Grid.RowDefinitions>
        <Frame Grid.Row="0" NavigationUIVisibility="Hidden" Name="Navigate" Content="{Binding Navigate.CurrentPage}"/>
        <Button Grid.Row="1" Width="100" Height="50" Content="clickback" Command="{Binding Navigate.NavigateCommandBack}"/>
        <Button Grid.Row="1" Width="100" Height="50" Content="click" Command="{Binding Navigate.NavigateCommand}" Margin="229,18,471,19"/>
    </Grid>
</Window>
